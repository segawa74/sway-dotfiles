# Default fedora config for sway
include /etc/sway/config

# Segawa74-Settings
input "type:touchpad" {
    tap enabled
    natural_scroll enabled
    pointer_accel 0
    drag_lock disabled
}

input "type:keyboard" {
    xkb_layout jp
}

exec fcitx5 -d
